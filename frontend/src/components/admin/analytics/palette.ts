/** Chart series colours — muted tones that sit with the navy/gold design
 *  system (defined as literals so Recharts' SVG `fill`/`stroke` resolve them
 *  in every browser). Keep in sync with the `--chart-*` notes in globals.css. */
export const CHART_COLORS = ['#3f3970', '#c99a2e', '#2a5b8c', '#2f6f4e', '#8a5a12', '#7a4b74'];
