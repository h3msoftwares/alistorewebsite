import { PagePlaceholder } from '@/components/page-placeholder';

// TODO: product list/table with stock + active status, edit/delete actions.
export default function AdminProductsPage() {
  return <PagePlaceholder title="Manage Products" />;
}
